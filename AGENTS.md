# 否定动作、标识符与分块恢复（v10 / 2026-09-10）

仅对内置/精确未修改的种子规则校验局部否定、ASCII标识符边界和“导出器”名词关系，不将“不读取Token”删去否定后当成攻击。双重否定、后续/重叠真实操作和自定义模式不得被豁免。规则未成立仍执行模型双审计；重复弱证据或失败仍555。纯编码值不是充分的操作证据，允许既有一次校核，但不能丢弃编码内容或自动放行。

同一请求、同一固定主审/自复核配置、完全相同的分块原文/位置/任务上下文/引用范围才可复用已完成全部双审计的allow。不能缓存主审单次allow、拒绝、失败或跨备用模型复用；未快照的外部复核/Fusion面板不复用。所有恢复共享原预算和期限；实际有效拒绝仍终态。诊断必须正确区分总期限与单次模型超时，并记录完成/复用块数。

正则性能优化只能用语法树证明的必要条件；保留Unicode SimpleFold等价类，遇到不能证明的表达式回退完整匹配，不能凭猜测关键词跳过。私钥赋值和转义/截断日志应脱敏，诊断去除不透明值不等于修改原始送审数据。短输出schema不替代运行时证据和类别校验。

# 正常本地开发与故障可观测性（v9 / 2026-09-10）

普通本地开发不能仅因 Token/Session/root/localhost 或提示词设计被归类为攻击。终端隐藏提示中由用户主动输入现成凭据，不等于提取隐藏秘密；讨论请求头认证方案，即使设计不佳，也不能虚构已实施认证绕过。此准确度边界优先于将一切“读取Token”都终态拒绝的旧字面解释。只对代码内置或精确未修改的已知种子规则校验动作关系（包括相邻表格行及否定提醒），操作员自定义规则不被覆盖。跳过一个不成立的命中不等于放行请求：后续同规则命中、全部其他规则、同样原文的模型双审计仍需执行。实际窃密、漏洞利用、绕过及混合禁用操作仍拒绝，不允许仅凭“本地/测试”声明绕过。

超时、取消、配置缺失/禁用、存储查询失败和不可能完成的分块计划必须分开记录，不记录驱动错误中的DSN/密码。超出双审计固定预算的长输入提前作为审计容量故障拒绝，不能删掉历史或宣称已审完；32MB入口上限不代表32MB文本可在120秒内完成审计。所有安全否决与故障保护保留，入口503过载不得当成用户违规。只以合成回归和真实模型回放分别报告成果。CSV为历史日志，禁止把旧拒绝记录改成已通过或提交真实请求/公司数据到公开仓库。

# 缺失引用恢复（v8 / 2026-09-10）

解析合法但未匹配原文的block/review尚未构成有效拒绝，可在相同送审文档/任务上下文下进行一次引用修复；不得将编造证据追加到用户数据。该次调用与既有操作校核共用候选一次额外调用上限，失败仍555，不能无限重试、模糊匹配或备用模型洗成allow。恢复后保留双审计/Fusion/完整历史和所有真实规则与有效操作拒绝。调用前校验送审文本与证据源一致，输入诊断只记录长度、阶段和带密钥指纹，不保存正文。生产模型效果与机制回归分开验收。

# 数据库同步与已提供凭据（v7 / 2026-09-10）

区分使用现成登录凭据与提取/披露秘密，区分已有 sudo 管理与漏洞提权，区分数据库备份/同步/迁移与非法外传。root、密码字段或全量拉取不能单独证明禁用操作；测试/自有声明同样不能豁免真实禁用操作。仅符合有限弱证据形态的相关类别候选在终态锁定前进行一次校核，保留全部原送审数据/任务锚点，重复弱证据和失败仍拒绝。所有真实规则否决和有效操作拒绝保留。禁止无脑给 SSH/sudo/数据库设置请求白名单。

审计完成必须结合已完成分块计数，不得将输入可提取或提前阻断冒充全部分块审完。小请求保留64 HTTP/32复核基础预算；长请求按分块数分配有限余量，绝对上限256 HTTP/128复核，跨重试共享已花费调用，不变更120秒期限或并发。必须测试60分块双审计、超限故障关闭和中途取消。

# 本机状态查看与凭据语法修正（2026-09-09）

当前准确度要求优先于旧“所有读取本地端口都阻断”的样例：被动查看本机连接/监听/PID不等于主动探测，仍须完整模型审计，不能按 netstat/ss 命令名直接拒绝，也不能作为全请求白名单。read-only guidance 是说明性质，不等于 read only the API key。默认凭据规则只在确认仍为原始种子模式时迁移；保留操作员自定义模式、启用状态和动作。主动扫描、实际凭据操作、混合禁用目的与审计失败的拒绝保护继续保留。只能说明机制回归结果，不能冒充生产 Qwen 准确率。

# 准确度修正边界（2026-09-09）

先判定实际禁用操作是否成立，再执行终态拒绝。包名/版本、目录文件名、普通源码检索命令只是资料形态，不能单独证明漏洞利用、安装提权或配置修改；也不能成为请求白名单。依赖、文件和只读检索候选需在同样数据及任务上下文下做至多一次操作证据校核，确定无禁用操作才可继续原正常双审计流程；再次不成立、超时或输出无效仍按审计故障拒绝。真实规则命中、已通过操作证据校核的模型 block/review 仍不可翻转。此限定是锁定前的证据准入修复，不是工程/授权豁免。

# 当前业务边界（2026-09-09 用户明确要求）

所有启用的 Cyber 规则命中必须终止转发并返回既有 HTTP 555；旧 action=review/allow 也不能形成白名单。自动化测试、授权、本人资产、内部工程、日志排障、CTF 等均不能豁免已经触发的 Cyber 能力。已通过输出/证据校验的模型 block/review 不得被后续模型或 Fusion 翻为 allow。普通非 Cyber 开发仍需负例回归；不得把所有测试或所有公网地址一律叫恶意。

业务禁止不等于证明恶意。解析、证据、覆盖、预算、超时错误必须记录为审计故障并 fail-closed，禁止伪造攻击证据。面向 Qwen3.8-27B 使用固定短策略、六字段 JSON、显式 thinking=false、主动分块、共享调用预算和 allow 新调用检查。不能把相同模型的多次结果称为独立专家共识，不能用 Mock 测试代替实际模型精度验收。此段取代下文或历史文档中的工程放行/候选可翻转策略。

# 项目开发约束

## P0：首要是输出不能崩溃

审计模型是不可靠的外部依赖。正常请求不得仅因模型输出的可恢复表示差异（如数字字符串、明确的定性 confidence、NONE 风险码）而被误报成恶意请求或拖垮响应链路。任何输出都不能导致 panic、无界递归、无限重试、失控内存增长或损坏 JSON/SSE 协议。先保证输出链路健壮，再迭代分类策略。

这不是关闭风控：禁止 parse-error => allow。不能猜测缺失的 decision、捏造证据或用默认高置信度批准未知结果。只做明确定义、可追踪、不会改变判定语义的规范化；剩余不确定性必须进入有界修复/语义复核/配置的故障策略。安全阻断、审计不可用、字段契约错误、网络失败必须分开记录，保持既有 555 对接契约。

## 输出契约与证据

- 区分 JSON 语法错误与合法 JSON 的字段类型/范围错误；不得把 `confidence:"high"` 简单标成 invalid_json。定性置信度不是校准后的概率，不得伪造成 0.95。
- 保留合法数值置信度的含义。拒绝非有限值、越界数值、矛盾判定、重复冲突字段及不支持的关键字段类型；不能从多个相互冲突的判定里挑一个 allow。
- 结构化输出约束不能代替响应校验。兼容性恢复必须有预算、错误分类和诊断，不能只换格式/加 tokens 后盲目重复。
- 平台输出指令只能在控制消息中；不得追加到待审 user 原文。引用定位有效不等于证明有害；保留当前意图/引用来源边界及候选拦截语义复核。
- 不得为 AOSP、ADB、公网 IP、GUI 手势、Agent 交互、工作流和数据库排障添加整段请求的全局放行规则。普通开发不能因为术语或环境本身被判定恶意，混合有害操作仍需审查。

## 每次修复的完成条件

先将用户事故转成脱敏回归，再修复。覆盖正常结果、表示漂移、缺字段、错误类型、截断、空响应、嵌套/重复/冲突 JSON、无效证据、超时、取消和复核失败。既证明可恢复正常输出不被意外拒绝，也证明异常输出不导致漏审放行。

运行 `go test -race -count=1 ./...`、`go vet ./...`、服务构建、升级脚本回归及既有 CI/E2E。解析器增加种子回归/模糊测试；改网关时验证 HTTP JSON/SSE 完整性和上游调用次数。禁止只改提示词或文档就声称问题已解决。

区分仓库提交、实际运行构建、机制回归、真实模型回放与生产验收。日志及健康信息应能识别运行版本/审计契约，不能仅凭 git pull 或 PR 已合并断言生产已升级。真实模型未测必须明确写出；不承诺零误报。敏感凭据和真实公司地址不得写入公开测试或日志。

本文件是项目的权威代理开发约束；`agent.MD` 为用户指定入口，指向本文件。
