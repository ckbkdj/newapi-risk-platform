# 代理开发入口

**P0：首要是输出不能崩溃。** 模型字段表示漂移不应拖垮正常业务，解析失败也不能被默认放行；必须进行有界、可追踪的规范化、复核与错误恢复。

本仓库完整开发约束以根目录 [AGENTS.md](AGENTS.md) 为准。修改前先阅读该文件。模型融合不得靠未校准的置信度加权，不得把多个同源回答宣称为独立验证；须保留分歧、调用预算及真实回放指标。诊断脚本默认只导出允许清单中的非敏感元数据，不自动上传。
