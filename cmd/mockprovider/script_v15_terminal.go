package main

// Inert terminal-veto marker for deterministic plumbing tests, not a judgment
// about any real script. Keep its source literal identical to the E2E fixture.
const v15TerminalQuote = "V15_SYNTHETIC_TERMINAL_VETO"
